const add = (x, y) => {
  return x + y;
}

const minus = (x, y) => {
  return x - y;
}

const multiply = (x, y) => {
  return x * y * 1;
}

const divide = (x, y) => {
  return x / y;
}

export {
  add,
  minus,
  multiply,
  divide,
}
